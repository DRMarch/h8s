# model-watch

Polls the Cluster's OpenAI-compatible `/v1/models` endpoint via Higress every 5 minutes and sends a Discord webhook when models are added or removed (upstream: [DRMarch/model-watch](https://github.com/DRMarch/model-watch)).
