apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-resources
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "-399"
spec:
  project: default
  source:
    repoURL: https://github.com/DRMarch/h8s.git
    targetRevision: HEAD
    path: security/vault/resources
  destination:
    server: https://kubernetes.default.svc
    namespace: vault
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - PruneLast=true
      - ServerSideApply=true
