apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-homelab-local
  namespace: argocd
spec:
  secretName: argocd-homelab-local-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: argocd.drmarchent.com
  dnsNames:
    - argocd.drmarchent.com
