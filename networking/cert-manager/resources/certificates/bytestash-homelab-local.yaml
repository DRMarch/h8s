apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bytestash-homelab-local
  namespace: bytestash
spec:
  secretName: bytestash-homelab-local-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: bytestash.drmarchent.com
  dnsNames:
    - bytestash.drmarchent.com
