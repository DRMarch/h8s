apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: searxng-homelab-local
  namespace: searxng
spec:
  secretName: searxng-homelab-local-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: search.drmarchent.com
  dnsNames:
    - search.drmarchent.com
