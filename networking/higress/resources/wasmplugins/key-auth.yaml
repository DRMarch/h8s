# GENERATED by terraform/higress-api-keys — do not edit by hand.
apiVersion: extensions.higress.io/v1alpha1
kind: WasmPlugin
metadata:
  name: key-auth
  namespace: higress-system
spec:
  url: oci://higress-registry.us-west-1.cr.aliyuncs.com/plugins/key-auth:2.0.0
  imagePullPolicy: IfNotPresent
  failStrategy: FAIL_CLOSE
  priority: 1000
  defaultConfigDisable: true
  matchRules:
    # External
    - domain:
        - llm.drmarchent.com
      config:
        consumers:
          - name: domain-admin-bearer
            credential: "Bearer ${secret.higress-system/higress-api-keys.domain-admin}"
          - name: domain-admin
            credential: ${secret.higress-system/higress-api-keys.domain-admin}
          - name: opencode-bearer
            credential: "Bearer ${secret.higress-system/higress-api-keys.opencode}"
          - name: opencode
            credential: ${secret.higress-system/higress-api-keys.opencode}
        keys:
          - authorization
          - x-api-key
        in_query: false
        in_header: true
    # Internal
    - domain:
        - higress-internal.higress-system.svc.cluster.local
      config:
        consumers:
          - name: model-watch-bearer
            credential: "Bearer ${secret.higress-system/higress-api-keys.model-watch}"
          - name: model-watch
            credential: ${secret.higress-system/higress-api-keys.model-watch}
          - name: open-webui-bearer
            credential: "Bearer ${secret.higress-system/higress-api-keys.open-webui}"
          - name: open-webui
            credential: ${secret.higress-system/higress-api-keys.open-webui}
        keys:
          - authorization
          - x-api-key
        in_query: false
        in_header: true
