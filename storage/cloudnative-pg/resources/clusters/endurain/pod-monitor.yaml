# https://cloudnative-pg.io/documentation/current/monitoring/
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cnpg-endurain-monitoring
spec:
  selector:
    matchLabels:
      "cnpg.io/cluster": cnpg-endurain
  podMetricsEndpoints:
  - port: metrics
