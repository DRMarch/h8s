apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cnpg-mlflow
  namespace: mlflow
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: cnpg-mlflow
  podMetricsEndpoints:
    - port: metrics
