apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: dragonfly
spec:
  podMetricsEndpoints:
    - port: admin
      path: /metrics
      interval: 30s
      scheme: http
  podTargetLabels:
    - app
  selector:
    matchLabels:
      app: searxng-dragonfly
