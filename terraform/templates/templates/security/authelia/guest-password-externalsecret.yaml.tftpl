apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authelia-guest-password
  namespace: authelia
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: vault-cluster-secretstore
    kind: ClusterSecretStore
  target:
    name: authelia-guest-password
    creationPolicy: Orphan
    deletionPolicy: Retain
  data:
    - secretKey: hash
      remoteRef:
        key: authelia/guest-password
        property: hash
