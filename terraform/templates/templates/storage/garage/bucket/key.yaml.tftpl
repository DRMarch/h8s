apiVersion: garage.rajsingh.info/v1beta1
kind: GarageKey
metadata:
  name: ${bucket_name}-key
spec:
  clusterRef:
    name: garage-default-cluster
  bucketPermissions:
    - bucketRef:
        name: ${bucket_name}
      read: true
      write: true
